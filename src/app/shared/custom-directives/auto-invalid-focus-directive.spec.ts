import { AutoInvalidFocusDirective } from './auto-invalid-focus-directive';

describe('AutoInvalidFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoInvalidFocusDirective();
    expect(directive).toBeTruthy();
  });
});
